{
  "name": "ws-server",
  "version": "1.0.0",
  "description": "Websocket debug tools",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "ws": "^6.1.4"
  }
}
